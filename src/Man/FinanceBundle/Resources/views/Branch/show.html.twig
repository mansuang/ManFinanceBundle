{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Branch</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Branch_name</th>
                <td>{{ entity.branchname }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('finance_branch') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('finance_branch_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
